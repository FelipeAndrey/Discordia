using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Unity.Rendering;

public class BarganhaVis : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        var VolumeProfile = GetComponent<Volume>();   
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //Execute the code after void update
    void LateUpdate()
    {

    }
}
